# Único provedor disponível nesta fase. Executa apenas no servidor.
AI_PROVIDER=mock

# Configure uma chave somente quando o provider correspondente existir.
# Nunca use o prefixo NEXT_PUBLIC_ para segredos.
# GEMINI_API_KEY=
# OPENAI_API_KEY=

# URL pública da aplicação, quando necessária para callbacks server-side.
APP_URL=http://localhost:3000
